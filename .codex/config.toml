[mcp_servers.substrate]
command = "deno"
args = [
  "run",
  "--allow-read",
  "--allow-write",
  "src/substrate-server.ts",
]
enabled = true
startup_timeout_sec = 10
tool_timeout_sec = 60

[mcp_servers.substrate.tools.query_session]
approval_mode = "approve"
